在一些信号处理中需要使用一些更高级的函数，在CMSIS中5.6版本以下无法支持这些函数，此文件夹是ARMDSP1.16的版本，可以满足大部分信号处理需求
使用时需要在MDK中关联路径，并且把source中的文件加入到文件夹中(主文件即可，有些是两个)，使用时加入"arm_math.h"即可。

如果遇到有些源码大量报错的情况，原因之一旧版的dsp库路径时关联错误，改为你的路径即可，或着加入项目文件夹使用相对路径。



